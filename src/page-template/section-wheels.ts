import { wrSystemConfig, SectionWheels } from '@wheelroom/core'
import { flowLight } from '../themes/flow/flow'

export const sectionWheels: SectionWheels = {
  defaultThemeId: 'flowLight',
  themes: {
    flowLight,
  },
  wrSystemConfig,
}
